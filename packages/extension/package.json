{
  "name": "vsc-name",
  "version": "1.0.0",
  "publisher": "youname",
  "description": "VS Code extension for Vue Webview Workspace",
  "main": "./dist/extension.js",
  "scripts": {
    "lint": "eslint \"src/**/*.{ts,vue}\"",
    "compile": "tsc -p ./tsconfig.json --skipLibCheck",
    "watch": "tsc -w -p ./tsconfig.json --skipLibCheck",
    "clean": "rm -rf ./dist",
    "package": "vsce package --no-dependencies --no-git-tag-version"
  },
  "engines": {
    "vscode": "^1.74.0"
  },
  "categories": [
    "Other"
  ],
  "activationEvents": [
    "onStartupFinished"
  ],
  "contributes": {
    "viewsContainers": {
      "activitybar": [
        {
          "id": "vue-webview-sidebar",
          "title": "Vue Webview Workspace",
          "icon": "resources/icon.png"
        }
      ]
    },
    "views": {
      "vue-webview-sidebar": [
        {
          "type": "webview",
          "id": "base-view-sidebar",
          "name": "Vue Webview Workspace",
          "icon": "resources/icon.png",
          "contextualTitle": "Vue Webview Workspace"
        }
      ]
    },
    "commands": [
      {
        "command": "vue-webview-workspace.helloWorld",
        "title": "Hello World"
      }
    ]
  },
  "dependencies": {
    "@vue-webview/libs": "workspace:*",
    "@vue-webview/view": "workspace:*"
  },
  "devDependencies": {
    "@types/vscode": "^1.74.0",
    "@vscode/vsce": "^2.15.0",
    "typescript": "^4.8.4"
  }
}
